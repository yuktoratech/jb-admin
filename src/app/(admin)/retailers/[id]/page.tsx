"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {Badge} from "@/components/ui/Badge";import {PageHeader} from "@/components/ui/PageHeader";import {ErrorState,LoadingState} from "@/components/ui/StateDisplay";import {retailerApi} from "@/features/accounts/account.api";import type {Retailer} from "@/features/accounts/account.types";import {getApiErrorMessage} from "@/lib/api";import {formatDateTime} from "@/lib/utils";
export default function RetailerDetail(){const {id}=useParams<{id:string}>();const [item,setItem]=useState<Retailer|null>(null),[error,setError]=useState<string|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{retailerApi.get(id).then(setItem).catch(e=>setError(getApiErrorMessage(e,"Unable to load Retailer."))).finally(()=>setLoading(false))},[id]);if(loading)return <LoadingState label="Loading Retailer"/>;if(error||!item)return <ErrorState message={error||"Retailer not found."}/>;const parent=typeof item.parentWholesaler==="string"?null:item.parentWholesaler;return <div className="mx-auto max-w-5xl"><PageHeader title={item.name} description="Read-only Retailer account details." actions={<Link className="text-sm font-semibold" href="/retailers">Back to Retailers</Link>}/><section className="rounded-lg border bg-white"><div className="flex justify-between border-b p-5"><h2 className="font-semibold">Account Information</h2><Badge tone={item.status==="active"?"active":"inactive"}>{item.status}</Badge></div><dl className="grid gap-5 p-5 sm:grid-cols-2 lg:grid-cols-3"><Info label="Email" value={item.email}/><Info label="Phone" value={item.phone||"—"}/><Info label="Retailer Discount" value={`${item.discountPercent}%`}/><Info label="Parent Wholesaler" value={parent?<Link className="text-[#7A1F2B] hover:underline" href={`/wholesalers/${parent._id}`}>{parent.name}</Link>:"Unavailable"}/><Info label="Created" value={formatDateTime(item.createdAt)}/><Info label="Updated" value={formatDateTime(item.updatedAt)}/><Info label="Last Login" value={formatDateTime(item.lastLoginAt)}/></dl></section><div className="mt-5 rounded-md border border-blue-100 bg-blue-50 p-4 text-sm text-blue-900">Admin access is read-only. Account changes are managed by the owning Wholesaler.</div></div>}
function Info({label,value}:{label:string;value:React.ReactNode}){return <div><dt className="text-xs uppercase tracking-wide text-neutral-500">{label}</dt><dd className="mt-1 break-words text-sm font-medium">{value}</dd></div>}
